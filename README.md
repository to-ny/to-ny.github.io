# Github homepage

Jekyll-based homepage hosted on GitHub Pages.

## Local Development

```bash
bundle install
bundle exec jekyll serve
```

Then visit `http://localhost:4000`

## Deployment

Push to `main` branch to deploy via GitHub Pages.
